<%- if expired?  %>
  <h3>The deadline for applications has passed. Please sign in to see existing application status.</h3>
<% else %>
  <div class="inner">
    <div class="panel panel-primary">
      <div class="panel-heading">Sign up</div>
      <div class="panel-body">
        <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :role => 'form' } ) do |f| %>

          <p>Please fill out the form below to begin the application process.</p>
          <hr />

          <%= custom_error_messages! %>

          <div class="form-group">

            <%= f.label :first_name, "First Name (example: George) " %>
            <%= f.text_field :first_name, :class => "form-control" %>

            <%= f.label :last_name, "Last Name (example: Washington)" %>
            <%= f.text_field :last_name, :class => "form-control" %>

            <%= f.label :email, "Email (example: george@whitehouse.gov)" %>
            <%= f.email_field :email, :class => "form-control" %>

            <%= f.label :phone, "Phone (example: 555-555-5555)" %>
            <%= f.text_field :phone, :class => "form-control" %>

            <%= f.label :password, "Password (8 or more characters please)" %>
            <%= f.password_field :password, :class => "form-control" %>

            <%= f.label :password_confirmation, "Password Confirmation (Same as in password)" %>
            <%= f.password_field :password_confirmation, :class => "form-control" %>

            <hr class='inner clear'>

            <div class='span11'>
              <p id='submit'>
                <%= f.submit "Sign up", :class => 'btn btn-ar btn-primary' %>
              </p>
              <%= render :partial => "applicants/shared/links" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
<% end %>
